{
    "id": "scan_edit_operation_1",
    "type": "SCAN_EDIT",
    "name": "Scan and Edit",
    "description": "Recursively scans a directory and it's submodules and makes changes to the files based on a provided goal.",
    "metadata": {},
    "schema": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "The path to the directory."
            },
            "goal": {
                "type": "string",
                "description": "The goal to accomplish while editing."
            }
        },
        "required": [
            "path",
            "goal"
        ]
    }
}